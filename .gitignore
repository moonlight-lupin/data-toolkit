# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Secrets / local env
.env
.env.*

# Built plugin bundles
*.plugin

# Self-test / generated artifacts (skills write deliverables into the USER's folders, not the repo)
*-selftest.html
*-selftest.xlsx
*_selftest*.xlsx
_selftest_clean.xlsx
**/--self-test

# OS cruft
.DS_Store
Thumbs.db

# Skill-creator eval workspaces (local iteration artifacts, never shipped)
_workings/

# Local WIP skills — kept out of the shipped plugin (data-lint skips git-ignored skills)
skills/mcp-guide/

# Environment artifact: `_rc/` copies appear next to .xlsx files wherever they're touched
_rc/
